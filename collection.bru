headers {
  Authorization: Bearer {{access_token}}
}

auth {
  mode: none
}

docs {
  # Request
  
  ## Body
  
  ```JSON
  {
  	// request
  }
  ```
  
  # Response
  
  ## Body
  
  ### 200 OK
  
  ```JSON
  {
    // success response
  }
  ```
  
  ### 400 Bad Request
  
  ```JSON
  // fail response (can be null)
  ```
  
}
